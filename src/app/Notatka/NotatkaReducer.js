export const NotatkaReducer = (
    state = "", action) =>
{
    switch (action.type) {
        case 'POKAZ_PRZYCISKI':
            return 
             "";
            default:
            return state;
    }
}